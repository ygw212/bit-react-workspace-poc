---
description: 'A second-app app.'
labels: ['app', 'react']
---

A React application for second-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use second-app 
```

To run the app locally, run the following:

```
bit run second-app 
```

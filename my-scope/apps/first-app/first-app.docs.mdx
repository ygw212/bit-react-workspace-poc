---
description: 'A first-app app.'
labels: ['app', 'react']
---

A React application for first-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use first-app 
```

To run the app locally, run the following:

```
bit run first-app 
```
